# Properties file with JDBC and JPA settings.
#
# Applied by <context:property-placeholder location="jdbc.properties"/> from
# various application context XML files (e.g., "applicationContext-*.xml").
# Targeted at system administrators, to avoid touching the context XML files.

#-------------------------------------------------------------------------------
# Common Settings

hibernate.generate_statistics = true
hibernate.show_sql            = false
#jpa.showSql                   = true

# Property that determines which Hibernate dialect to use
# (only applied with "applicationContext-hibernate.xml")
#hibernate.dialect=org.hibernate.dialect.HSQLDialect

# Property that determines which JPA DatabasePlatform to use with TopLink Essentials
#jpa.databasePlatform = org.springframework.samples.petclinic.toplink.EssentialsHSQLPlatformWithNativeSequence

# Property that determines which database to use with an AbstractJpaVendorAdapter
#jpa.database = HSQL

#-------------------------------------------------------------------------------
# MySQL Settings

jdbc.driverClassName=com.mysql.jdbc.Driver
jdbc.url=jdbc:mysql://localhost:3306/yoda
jdbc.username=root
jdbc.password=root

#taklip.com
#jdbc.driverClassName=com.mysql.jdbc.Driver
#jdbc.url=jdbc:mysql://aa1ke0fc9ou1rgj.c1whokddqehh.ap-southeast-1.rds.amazonaws.com:3306/ebdb
#jdbc.username=dbadmin
#jdbc.password=27-a8171i323

#iaskar.com
#jdbc.driverClassName=com.mysql.jdbc.Driver
#jdbc.url=jdbc:mysql://aapmdsy4ukgjhj.c1whokddqehh.ap-southeast-1.rds.amazonaws.com:3306/ebdb
#jdbc.username=dbadmin
#jdbc.password=27-a8171i323

# redis
redis.host=localhost
redis.port=6379
redis.password=
redis.timeout=100000
redis.pool.maxIdle=300
redis.pool.maxTotal=600
redis.pool.maxWaitMillis=1000
redis.pool.testOnBorrow=true

# Property that determines which Hibernate dialect to use
# (only applied with "applicationContext-hibernate.xml")
hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Property that determines which JPA DatabasePlatform to use with TopLink Essentials
#jpa.databasePlatform = oracle.toplink.essentials.platform.database.MySQL4Platform

# Property that determines which database to use with an AbstractJpaVendorAdapter
#jpa.database = MYSQL